"""Pure extraction functions — no I/O, no classes.

Each module provides extraction functions that take HTML strings and config dicts
and return deadline data. The Scrapy spider handles all fetching.
"""
